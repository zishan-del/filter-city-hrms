# FILTER CITY HRMS Android build

This directory is reserved for the Android wrapper build. The Android app will use the same deployed HRMS web application and cloud API; no separate employee database is created.

Build target: Android APK for testing, then Android App Bundle (AAB) for Google Play.
