# FILTER CITY HRMS App Build

Development branch: `app-build`

Stable baseline: `stable-final` (do not modify for app experiments).

The app build keeps the existing cloud API and PostgreSQL data source. App-shell work is isolated from HRMS business logic.
