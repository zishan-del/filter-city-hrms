# FILTER CITY HRMS

New clean HRMS application.

Architecture:

Browser (PC/mobile) → Vercel API → PostgreSQL cloud database

The cloud database is the single source of truth. Browser local storage is never used as the authoritative data store.
